{
  "name": "ainightsweb",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "assets:speaker-announcements": "node scripts/generate-speaker-announcements.mjs",
    "build": "node scripts/fetch-legal.mjs && npm run assets:speaker-announcements && astro build && node scripts/postbuild.mjs",
    "preview": "node scripts/fetch-legal.mjs && npm run assets:speaker-announcements && SHOW_SCHEDULED=1 astro build && node scripts/postbuild.mjs && astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.1.1",
    "marked": "^18.0.6",
    "sharp": "^0.33.5"
  }
}
