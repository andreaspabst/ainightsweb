---
import { imgVariant } from '../lib/images';
import { categoryLabel } from '../lib/blog';
import type { CollectionEntry } from 'astro:content';

export interface Props {
  posts: CollectionEntry<'blog'>[];
}
const { posts } = Astro.props;

function fmtDate(d: Date) {
  return d.toLocaleDateString('de-DE', { day: '2-digit', month: 'long', year: 'numeric' });
}

// Nur im Dev-Modus sichtbar: Kennzeichnung für Drafts/geplante Beiträge
const previewLabel = (p: CollectionEntry<'blog'>) =>
  p.data.draft ? 'Draft' : p.data.pubDate.getTime() > Date.now() ? `Geplant · ${p.data.pubDate.toLocaleDateString('de-DE')}` : null;
---

<div class="grid">
  {posts.map((p) => (
    <article class="post card">
      <a href={`/blog/${p.id}/`} class="cover">
        {p.data.image?.src && <img src={imgVariant(p.data.image.src, 640)} alt={p.data.image.alt ?? p.data.title} loading="lazy" width="640" height="400" />}
        <span class="cat-badge">{categoryLabel(p.data.category)}</span>
      </a>
      <div class="body">
        <p class="date">
          {fmtDate(p.data.pubDate)}
          {previewLabel(p) && <span class="preview-badge">{previewLabel(p)}</span>}
        </p>
        <h2><a href={`/blog/${p.id}/`}>{p.data.title}</a></h2>
        <p class="excerpt">{p.data.description}</p>
        {p.data.tags && p.data.tags.length > 0 && (
          <p class="tags">{p.data.tags.map((t) => <span class="tag">#{t}</span>)}</p>
        )}
        <a href={`/blog/${p.id}/`} class="more">Weiterlesen →</a>
      </div>
    </article>
  ))}
</div>

<style>
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
  }
  .post {
    display: flex;
    flex-direction: column;
    overflow: hidden;
    border-radius: 26px;
    transition: transform 0.25s ease, border-color 0.25s ease, box-shadow .25s ease;
  }
  .post:hover {
    transform: translateY(-7px);
    border-color: var(--glass-border-hover);
    box-shadow: 0 32px 80px rgba(2,0,18,.48), inset 0 1px rgba(255,255,255,.16);
  }
  .cover {
    position: relative;
    display: block;
    aspect-ratio: 16 / 10;
    background: var(--bg-elevated);
    overflow: hidden;
  }
  .cover img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform .5s ease, filter .4s ease;
  }
  .post:hover .cover img { transform:scale(1.045);filter:saturate(1.12); }
  .cover::after { content:'';position:absolute;inset:0;background:linear-gradient(180deg,transparent 52%,rgba(9,1,25,.65));pointer-events:none; }
  .cat-badge {
    position: absolute;
    top: 0.9rem;
    left: 0.9rem;
    background: var(--gradient-accent);
    color: #fff;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    padding: 0.3rem 0.7rem;
    border-radius: 999px;
    z-index: 2;
    border:1px solid rgba(255,255,255,.22);
    backdrop-filter:blur(14px);
  }
  .body {
    padding: 1.2rem 1.3rem 1.4rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    flex: 1;
  }
  .date {
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--blue-bright);
  }
  .preview-badge {
    display: inline-block;
    margin-left: 0.5rem;
    padding: 0.15rem 0.6rem;
    border-radius: 999px;
    border: 1px dashed var(--magenta-bright);
    color: var(--magenta-bright);
    font-size: 0.72rem;
    letter-spacing: 0.03em;
    text-transform: uppercase;
  }
  h2 {
    font-size: 1.3rem;
    letter-spacing: -.035em;
  }
  h2 a {
    color: var(--text-primary);
  }
  h2 a:hover {
    color: var(--magenta-bright);
  }
  .excerpt {
    font-size: 0.92rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }
  .tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  .tag {
    font-size: 0.78rem;
    color: var(--text-muted);
  }
  .more {
    margin-top: auto;
    padding-top: 0.6rem;
    font-weight: 700;
    font-size: 0.9rem;
    color: var(--magenta-bright);
  }
</style>
